1）运行方式: 
	切换到到当前的目录内，python jstree_web.py 8000
	然后访问: 127.0.0.1:8000/

										enjoy it! zhangjunfeng.


2)对原有使用插件的修改:
对jquery.jstree.js 1.0版本的改动：
		注释掉了3019-3025行。



3)杂乱的小记:
1,

	给$("demo a")元素添加一个click事件后，可能影响到了jstree自身的create函数，使create函数找不到了当前被选的节点，解决方法是在create函数里面传入用jquery找到的节点。
2，
删除操作。
现在实现的删除是删除某个组中，某个用户。
接下来可能的需求是删除修改右键菜单让用户选择是从系统中删除这个用户，还是把这个用户从这个组中删除。
3,
	改名操作。

